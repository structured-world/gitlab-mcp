name = "gitlab-mcp-docs-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

routes = [
  { pattern = "docs.gitlab-mcp.sw.foundation/api/*", zone_name = "sw.foundation" }
]

# KV namespace for rate limiting (create in CF dashboard, paste ID here)
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = "<PASTE_KV_NAMESPACE_ID>"
